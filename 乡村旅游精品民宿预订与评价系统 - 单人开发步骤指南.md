# 乡村旅游精品民宿预订与评价系统 - 单人开发步骤指南

根据课程设计指导书要求，**单人一组**的开发步骤。我将按照**3周时间**规划，帮你一步步完成系统。

---

## 📅 整体时间规划（3周）

| 时间      | 阶段      | 主要任务                                 |
| --------- | --------- | ---------------------------------------- |
| **第1周** | 分析+设计 | 选题确认、需求分析、数据库设计、原型设计 |
| **第2周** | 编码实现  | 基础架构搭建、各模块CRUD实现             |
| **第3周** | 测试+文档 | 系统测试、bug修复、撰写报告、准备答辩    |

---

## 🚀 第1周：分析设计阶段（7天）

### 第1天：环境准备 + 项目初始化

```bash
# 1. 创建项目目录
mkdir HomestayBookingSystem
cd HomestayBookingSystem

# 2. 创建标准目录结构
mkdir -p src/main/java/com/booking/{model,dao,service,view,util}
mkdir -p src/main/resources
mkdir -p sql
mkdir -p docs
mkdir -p lib
mkdir -p screenshots

# 3. 下载MySQL JDBC驱动到lib目录
# 访问：https://mvnrepository.com/artifact/mysql/mysql-connector-java/5.1.49

# 4. 初始化Git仓库
git init
echo "# 乡村旅游精品民宿预订与评价系统" > README.md
git add .
git commit -m "init: 项目初始化"
```

### 第2天：数据库设计（8张表）

创建SQL脚本文件 `sql/create_tables.sql`：

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS homestay_booking 
CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;

USE homestay_booking;

-- 1. 用户表
CREATE TABLE users (
    user_id INT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    role ENUM('ADMIN', 'HOST', 'GUEST') NOT NULL,
    real_name VARCHAR(50),
    phone VARCHAR(20),
    email VARCHAR(100),
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    status TINYINT DEFAULT 1
);

-- 2. 民宿表
CREATE TABLE homestays (
    homestay_id INT PRIMARY KEY AUTO_INCREMENT,
    host_id INT NOT NULL,
    name VARCHAR(100) NOT NULL,
    address VARCHAR(200) NOT NULL,
    city VARCHAR(50),
    description TEXT,
    facilities TEXT,
    phone VARCHAR(20),
    rating DECIMAL(2,1) DEFAULT 0,
    status TINYINT DEFAULT 1,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (host_id) REFERENCES users(user_id)
);

-- 3. 房间表
CREATE TABLE rooms (
    room_id INT PRIMARY KEY AUTO_INCREMENT,
    homestay_id INT NOT NULL,
    room_number VARCHAR(20) NOT NULL,
    room_type ENUM('SINGLE','DOUBLE','TWIN','SUITE','FAMILY') NOT NULL,
    max_people INT NOT NULL,
    price DECIMAL(10,2) NOT NULL,
    status ENUM('AVAILABLE','BOOKED','MAINTENANCE') DEFAULT 'AVAILABLE',
    description TEXT,
    FOREIGN KEY (homestay_id) REFERENCES homestays(homestay_id)
);

-- 4. 预订订单表（核心）
CREATE TABLE reservations (
    reservation_id INT PRIMARY KEY AUTO_INCREMENT,
    reservation_no VARCHAR(32) NOT NULL UNIQUE,
    room_id INT NOT NULL,
    guest_id INT NOT NULL,
    check_in_date DATE NOT NULL,
    check_out_date DATE NOT NULL,
    guests_count INT NOT NULL,
    total_price DECIMAL(10,2) NOT NULL,
    status ENUM('PENDING','PAID','CONFIRMED','CHECKED_IN','COMPLETED','CANCELLED') DEFAULT 'PENDING',
    guest_name VARCHAR(50) NOT NULL,
    guest_phone VARCHAR(20) NOT NULL,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (room_id) REFERENCES rooms(room_id),
    FOREIGN KEY (guest_id) REFERENCES users(user_id)
);

-- 5. 入住记录表
CREATE TABLE checkin_records (
    record_id INT PRIMARY KEY AUTO_INCREMENT,
    reservation_id INT NOT NULL UNIQUE,
    actual_check_in DATETIME,
    actual_check_out DATETIME,
    deposit DECIMAL(10,2) DEFAULT 0,
    FOREIGN KEY (reservation_id) REFERENCES reservations(reservation_id)
);

-- 6. 评价表
CREATE TABLE reviews (
    review_id INT PRIMARY KEY AUTO_INCREMENT,
    reservation_id INT NOT NULL UNIQUE,
    guest_id INT NOT NULL,
    rating TINYINT CHECK (rating BETWEEN 1 AND 5),
    comment TEXT,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (reservation_id) REFERENCES reservations(reservation_id),
    FOREIGN KEY (guest_id) REFERENCES users(user_id)
);

-- 7. 支付记录表
CREATE TABLE payments (
    payment_id INT PRIMARY KEY AUTO_INCREMENT,
    payment_no VARCHAR(32) NOT NULL UNIQUE,
    reservation_id INT NOT NULL,
    amount DECIMAL(10,2) NOT NULL,
    payment_method ENUM('CASH','WECHAT','ALIPAY') NOT NULL,
    status ENUM('PENDING','SUCCESS','FAILED') DEFAULT 'PENDING',
    pay_time DATETIME,
    FOREIGN KEY (reservation_id) REFERENCES reservations(reservation_id)
);

-- 8. 定价规则表
CREATE TABLE pricing_rules (
    rule_id INT PRIMARY KEY AUTO_INCREMENT,
    homestay_id INT,
    rule_name VARCHAR(100) NOT NULL,
    rule_type ENUM('WEEKEND','HOLIDAY','SEASON') NOT NULL,
    discount_type ENUM('PERCENT','FIXED') DEFAULT 'PERCENT',
    discount_value DECIMAL(5,2) NOT NULL,
    status TINYINT DEFAULT 1,
    FOREIGN KEY (homestay_id) REFERENCES homestays(homestay_id)
);

-- 创建索引
CREATE INDEX idx_reservations_dates ON reservations(check_in_date, check_out_date);
CREATE INDEX idx_reservations_room_id ON reservations(room_id);
CREATE INDEX idx_rooms_status ON rooms(status);
```

### 第3天：创建触发器 + 存储过程

创建 `sql/triggers_procedures.sql`：

```sql
-- 触发器：订单取消时释放房间
DELIMITER //
CREATE TRIGGER after_reservation_cancelled
AFTER UPDATE ON reservations
FOR EACH ROW
BEGIN
    IF NEW.status = 'CANCELLED' AND OLD.status != 'CANCELLED' THEN
        UPDATE rooms SET status = 'AVAILABLE' WHERE room_id = NEW.room_id;
    END IF;
END//

-- 存储过程：检查房间可用性
CREATE PROCEDURE sp_check_room_availability(
    IN p_room_id INT,
    IN p_check_in DATE,
    IN p_check_out DATE,
    OUT p_available BOOLEAN
)
BEGIN
    SELECT COUNT(*) INTO @conflict
    FROM reservations
    WHERE room_id = p_room_id
      AND status IN ('PAID', 'CONFIRMED', 'CHECKED_IN')
      AND check_in_date < p_check_out 
      AND check_out_date > p_check_in;
    
    SET p_available = (@conflict = 0);
END//
DELIMITER ;
```

### 第4天：插入测试数据

创建 `sql/test_data.sql`：

```sql
-- 插入测试用户（密码都是123456的MD5值）
INSERT INTO users (username, password, role, real_name, phone) VALUES
('admin', 'e10adc3949ba59abbe56e057f20f883e', 'ADMIN', '管理员', '13800001111'),
('host1', 'e10adc3949ba59abbe56e057f20f883e', 'HOST', '王老板', '13912345678'),
('guest1', 'e10adc3949ba59abbe56e057f20f883e', 'GUEST', '张三', '15812345678'),
('guest2', 'e10adc3949ba59abbe56e057f20f883e', 'GUEST', '李四', '15987654321');

-- 插入民宿
INSERT INTO homestays (host_id, name, address, city, description, phone) VALUES
(2, '云中山居', '北京怀柔区雁栖镇', '北京', '山景民宿，环境幽静', '010-12345678'),
(2, '海边小筑', '秦皇岛北戴河区', '秦皇岛', '距离海边步行5分钟', '0335-1234567');

-- 插入房间
INSERT INTO rooms (homestay_id, room_number, room_type, max_people, price, description) VALUES
(1, '101', 'DOUBLE', 2, 388.00, '山景大床房'),
(1, '102', 'TWIN', 2, 388.00, '山景标准间'),
(1, '201', 'SUITE', 3, 688.00, '山景套房'),
(2, 'A01', 'DOUBLE', 2, 588.00, '海景大床房'),
(2, 'A02', 'FAMILY', 4, 888.00, '家庭海景房');

-- 插入预订订单（大量测试数据）
INSERT INTO reservations (reservation_no, room_id, guest_id, check_in_date, check_out_date, 
                         guests_count, total_price, status, guest_name, guest_phone) VALUES
('20240601001', 1, 3, '2024-06-01', '2024-06-03', 2, 776.00, 'COMPLETED', '张三', '15812345678'),
('20240615002', 2, 3, '2024-06-15', '2024-06-18', 2, 1164.00, 'COMPLETED', '张三', '15812345678'),
('20240701003', 4, 4, '2024-07-01', '2024-07-05', 2, 2352.00, 'PAID', '李四', '15987654321'),
('20240720004', 5, 4, '2024-07-20', '2024-07-25', 4, 4440.00, 'CONFIRMED', '李四', '15987654321'),
('20240801005', 1, 3, '2024-08-01', '2024-08-03', 2, 776.00, 'PENDING', '张三', '15812345678');

-- 插入评价
INSERT INTO reviews (reservation_id, guest_id, rating, comment) VALUES
(1, 3, 5, '非常满意！房间干净，风景很好'),
(2, 3, 4, '环境不错，就是上山路有点难走');
```

### 第5-7天：需求分析 + 系统设计 + 模块划分

**撰写文档（放入docs文件夹）：**

1. **需求分析文档**：列出所有功能点
2. **系统模块图**：手绘或用工具画功能结构图
3. **ER图**：画8张表的实体关系图
4. **界面原型**：画主要界面的草图

**功能模块划分：**
```
乡村旅游精品民宿预订与评价系统
├── 1. 用户模块
│   ├── 登录/注册
│   ├── 个人信息管理
│   └── 权限控制（管理员/民宿主/游客）
├── 2. 民宿管理模块
│   ├── 民宿信息管理（CRUD）
│   ├── 房间管理（CRUD）
│   └── 房间状态管理
├── 3. 预订模块（核心）
│   ├── 房间查询（按日期/人数）
│   ├── 预订下单（事务处理）
│   ├── 预订冲突检测
│   └── 订单状态管理
├── 4. 入住管理模块
│   ├── 入住登记
│   ├── 退房处理
│   └── 历史记录查询
├── 5. 评价模块
│   ├── 评价发布
│   ├── 评价查看
│   └── 评分统计
├── 6. 支付模块
│   ├── 支付记录
│   └── 账单查询
└── 7. 统计报表模块
    ├── 入住率统计
    ├── 收入统计
    └── 热门房间排行
```

**Git提交：**
```bash
git add .
git commit -m "docs: 完成需求分析和数据库设计"
```

---

## 💻 第2周：编码实现阶段（7天）

### 第1-2天：基础架构搭建

**创建工具类：**
1. `DBUtil.java` - 数据库连接（含事务管理）
2. `MD5Util.java` - 密码加密
3. `DateUtil.java` - 日期处理

**提交记录：**
```bash
git add src/main/java/com/booking/util/
git commit -m "feat: 添加工具类 DBUtil、MD5Util、DateUtil"
```

### 第3-4天：实体类 + DAO层（8张表）

按顺序创建：
1. `User.java` + `UserDAO.java` + `UserDAOImpl.java`
2. `Homestay.java` + `HomestayDAO.java`
3. `Room.java` + `RoomDAO.java`
4. `Reservation.java` + `ReservationDAO.java`（核心，包含分页查询）
5. `CheckinRecord.java` + `CheckinRecordDAO.java`
6. `Review.java` + `ReviewDAO.java`
7. `Payment.java` + `PaymentDAO.java`
8. `PricingRule.java` + `PricingRuleDAO.java`

**关键代码示例 - ReservationDAOImpl中的分页查询：**
```java
public List<Reservation> findByPage(int pageNum, int pageSize, String keyword) {
    List<Reservation> list = new ArrayList<>();
    String sql = "SELECT r.*, rm.room_number, h.name as homestay_name " +
                 "FROM reservations r " +
                 "JOIN rooms rm ON r.room_id = rm.room_id " +
                 "JOIN homestays h ON rm.homestay_id = h.homestay_id " +
                 "WHERE r.guest_name LIKE ? OR h.name LIKE ? " +
                 "ORDER BY r.create_time DESC " +
                 "LIMIT ?, ?";
    int offset = (pageNum - 1) * pageSize;
    // 执行查询...
    return list;
}
```

**提交记录：**
```bash
# 每完成一个DAO就提交一次
git add src/main/java/com/booking/model/User.java
git add src/main/java/com/booking/dao/UserDAO.java
git commit -m "feat: 完成User实体类和DAO层"

git add src/main/java/com/booking/model/Reservation.java
git add src/main/java/com/booking/dao/ReservationDAO.java
git commit -m "feat: 完成Reservation实体类和DAO层（含分页查询）"
# ... 以此类推
```

### 第5天：Service层（业务逻辑）

创建：
1. `UserService.java` - 登录验证、注册
2. `ReservationService.java` - 预订核心业务（冲突检测、事务）

**关键代码 - 预订业务（含事务）：**
```java
public boolean createReservation(Reservation reservation) {
    Connection conn = null;
    try {
        conn = DBUtil.getConnection();
        conn.setAutoCommit(false);
        
        // 1. 检查房间可用性
        if (!checkRoomAvailable(reservation.getRoomId(), 
                               reservation.getCheckInDate(), 
                               reservation.getCheckOutDate())) {
            return false;
        }
        
        // 2. 生成订单号
        reservation.setReservationNo(generateOrderNo());
        
        // 3. 保存订单
        reservationDAO.insert(reservation);
        
        // 4. 更新房间状态
        roomDAO.updateStatus(reservation.getRoomId(), "BOOKED");
        
        conn.commit();
        return true;
    } catch (Exception e) {
        conn.rollback();
        e.printStackTrace();
        return false;
    } finally {
        DBUtil.closeConnection();
    }
}
```

**提交记录：**
```bash
git add src/main/java/com/booking/service/
git commit -m "feat: 完成Service业务层，实现预订事务处理"
```

### 第6-7天：GUI界面开发

使用Swing/JavaFX创建：
1. 登录界面
2. 主界面（含菜单）
3. 民宿管理界面
4. 房间管理界面
5. 预订查询界面（分页+关键字查询）
6. 下单界面（含日期冲突提示）
7. 评价界面
8. 统计报表界面

**提交记录：**
```bash
# 每完成一个界面就提交
git add src/main/java/com/booking/view/LoginView.java
git commit -m "feat: 完成登录界面"

git add src/main/java/com/booking/view/ReservationView.java
git commit -m "feat: 完成预订查询界面（分页+关键字查询）"
# ... 以此类推
```

---

## 📝 第3周：测试+文档阶段（7天）

### 第1-2天：系统测试

编写测试用例，测试所有功能：
1. 登录注册测试
2. 民宿/房间CRUD测试
3. 预订测试（含冲突检测）
4. 评价测试
5. 统计报表测试

**创建测试文档** `docs/test_cases.md`

**提交记录：**
```bash
git add docs/test_cases.md
git commit -m "test: 添加测试用例文档"

# 修复bug后提交
git add src/main/java/com/booking/service/ReservationService.java
git commit -m "fix: 修复预订日期冲突检测逻辑"
```

### 第3-5天：撰写课程设计报告

按照模板完成报告（30-60页）：
- 封面（按附件二格式）
- 任务书
- 成绩评定表
- 目录
- **正文**：
  - 概述（背景、意义）
  - 相关技术介绍（Java、JDBC、MySQL、Swing、Git）
  - 需求分析（功能需求+非功能需求）
  - 系统概要设计（架构图、模块图）
  - 系统详细设计（ER图、8张表的三线表）
  - 系统实现（截图+代码片段）
  - 系统测试（测试用例表）
  - 总结与展望
- 参考文献（至少10个，含4个英文）
- 附录（AI对话截图，如果有用AI）

### 第6天：准备答辩PPT

PPT包含：
1. 项目背景与意义
2. 系统功能模块
3. 数据库设计（ER图）
4. 核心功能演示（预订+冲突检测）
5. 关键技术点（事务处理、分页、触发器）
6. 创新点（如果有）
7. 总结与心得

### 第7天：整理提交材料

按照指导书要求整理：

```
班级_学号_姓名_电子材料/
├── 学号_姓名_课程设计报告.docx
├── 1.project/
│   ├── HomestayBookingSystem/  (整个工程文件夹)
│   ├── database.sql            (导出的SQL文件)
│   └── README.md                (重建说明)
├── 2.release/                  (可选，可执行程序)
├── 3.other/                     (其他文件)
└── 4.workpics/                  (工作照片，至少5张)
    ├── 2025_02_17_环境搭建.jpg
    ├── 2025_02_20_数据库设计.jpg
    ├── 2025_02_25_编码开发.jpg
    ├── 2025_03_01_界面调试.jpg
    └── 2025_03_05_系统测试.jpg
```

**压缩提交：**
```bash
7z a 班级_学号_姓名_电子材料.7z 班级_学号_姓名_电子材料/
```

---

## ✅ 验收检查清单

### 程序方面（40分）
- [ ] 8张表全部创建
- [ ] 跨表关联查询（如预订关联房间、民宿）
- [ ] 事务处理（预订操作）
- [ ] 索引（在常用查询字段上）
- [ ] 触发器（订单取消释放房间）
- [ ] 存储过程（检查房间可用性）
- [ ] 分页功能（预订列表）
- [ ] 排序功能（按时间、价格）
- [ ] 关键字查询（按民宿名、客人名）
- [ ] 面向对象设计（继承、多态）
- [ ] 原生JDBC（不使用ORM）
- [ ] 界面操作流畅

### Git方面
- [ ] git log能体现每天的工作量
- [ ] 提交信息规范（feat/fix/docs等）
- [ ] 代码托管在GitHub/Gitee

### 文档方面（40分）
- [ ] 报告30-60页
- [ ] 需求分析完整
- [ ] 系统设计详细
- [ ] 数据库设计（ER图+8张表的三线表）
- [ ] 开发与测试环节
- [ ] 工作总结
- [ ] 参考文献≥10个（含4英文）

### 答辩方面（10分）
- [ ] 系统能正常运行
- [ ] 能流畅演示核心功能
- [ ] 能回答老师提问

### 创新加分（10分）
- [ ] 数据库连接池
- [ ] JDBC封装
- [ ] MVC模式
- [ ] 图表统计
- [ ] Docker部署

---

## 📌 每日工作记录模板

在 `docs/worklog.md` 中记录：

```markdown
# 工作日志

## 2025-02-17 周一
- 完成：项目初始化、Git仓库创建
- 遇到的问题：无
- 明日计划：数据库设计
- 提交：init: 项目初始化

## 2025-02-18 周二
- 完成：8张表设计、创建SQL脚本
- 遇到的问题：外键约束顺序问题
- 解决方法：先建父表再建子表
- 明日计划：创建触发器、存储过程
- 提交：feat: 完成数据库表创建
```

---

按照这个步骤，**每天完成一个小目标**，3周时间完全可以完成！有问题随时问我。